/** SQLite application_id for "AMBT" (Ambient Agent application state). */
export const APPLICATION_DATABASE_ID = 0x414d4254;

/** Current app-owned SQLite schema. Version 0 is the shipped unversioned predecessor. */
export const APPLICATION_DATABASE_SCHEMA_VERSION = 2;
