# Capabilities own agent-facing abilities

Ambience grows through capability-owned bundles rather than a flat catalogue of provider-shaped tools or one universal command tool. Each Capability owns the Skill that guides its judgment and the focused Tools that provide its direct abilities, while provider adapters remain private implementation details; the initial production slice therefore replaces the existing GitHub proof with Issue Management instead of preserving or renaming the proof path.
